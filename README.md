# Concert Booking Service

Сервис бронирования билетов на концерты с event-driven архитектурой.

Микросервис для управления концертами и бронированием билетов. Реализует безопасное транзакционное управление остатками мест с учетом конкурентного доступа, асинхронные уведомления через Apache Kafka, кэширование в Redis для снижения нагрузки на базу данных и JWT-аутентификацию.

## Технический стек

*   Язык: Go 1.25
*   База данных: PostgreSQL 15
*   Кэширование: Redis 7.4
*   Messaging: Apache Kafka 7.4.0 + Zookeeper
*   Роутер: Chi v5
*   Миграции: Goose
*   Контейнеризация: Docker + Docker Compose

## Как запустить
М
Для работы сервиса необходимы установленные Docker и Docker Compose.

1. Склонируйте репозиторий и перейдите в директорию проекта:
```bash
git clone https://github.com/yohnnn/booking_service.git
cd booking_service
```

2. Создайте `.env` файл на основе шаблона:
```bash
cp .env.template .env
```

3. Запустите проект командой:
```bash
make docker-up
```

После успешного запуска будут доступны:
*   API Сервиса: http://localhost:8080
*   Kafka (внешний): localhost:29092
*   PostgreSQL: localhost:5432
*   Redis: localhost:6379

## Команды Makefile

В проекте предусмотрен Makefile для удобства разработки:

| Команда | Описание |
| :--- | :--- |
| `make docker-up` | Поднять все сервисы в Docker (с миграциями) |
| `make docker-down` | Остановить и удалить контейнеры |
| `make consumer` | Запустить Kafka consumer для обработки событий бронирования |

## API Эндпоинты

### Authentication
| Метод | Путь | Описание |
| :--- | :--- | :--- |
| POST | `/api/auth/register` | Регистрация нового пользователя |
| POST | `/api/auth/login` | Авторизация (получение JWT токена) |

### Concerts
| Метод | Путь | Описание | Авторизация |
| :--- | :--- | :--- | :--- |
| GET | `/api/concerts` | Получить список всех концертов | Нет |
| GET | `/api/concerts/{id}` | Получить информацию о конкретном концерте | Нет |

### Bookings
| Метод | Путь | Описание | Авторизация |
| :--- | :--- | :--- | :--- |
| POST | `/api/bookings` | Создать бронирование (отправляет событие в Kafka) | Да |
| GET | `/api/bookings` | Получить все бронирования пользователя | Да |

## Структура проекта
```
├── cmd
│   ├── consumer          # Kafka consumer для обработки событий
│   └── server            # HTTP сервер
├── internal
│   ├── app              # Инициализация приложения
│   ├── cache            # Redis кэш для концертов
│   ├── config           # Конфигурация из ENV
│   ├── dto              # Data Transfer Objects
│   ├── event            # Kafka producer и структуры событий
│   ├── handler          # HTTP handlers (v1 API)
│   ├── middleware       # HTTP middleware (auth, logger)
│   ├── models           # Доменные модели
│   ├── repository       # Слой работы с БД (PostgreSQL)
│   └── service          # Бизнес-логика
├── migrations           # SQL миграции (Goose)
└── docker-compose.yml   # Оркестрация сервисов
```

## Основные возможности

1.  **Event-Driven Architecture:** При создании бронирования событие отправляется в Kafka. Consumer обрабатывает события и может отправлять уведомления (email, push и т.д.).
2.  **Транзакционная безопасность:** Создание бронирования, уменьшение доступных мест и отправка события выполняются в одной транзакции с изоляцией.
3.  **Кэширование:** Список концертов кэшируется в Redis с TTL 5 минут для снижения нагрузки на PostgreSQL.
4.  **JWT Authentication:** Защищенные эндпоинты требуют валидный JWT токен (TTL 24 часа).
5.  **Graceful Shutdown:** Корректное закрытие соединений с БД, Redis и Kafka при остановке сервиса.
6.  **Clean Architecture:** Разделение на слои (handler → service → repository) для тестируемости и масштабируемости.

## Примеры использования

### 1. Регистрация пользователя
```bash
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "securepassword123"
  }'
```

### 2. Авторизация
```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "securepassword123"
  }'
```

Ответ:
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

### 3. Получить список концертов
```bash
curl http://localhost:8080/api/concerts
```

### 4. Создать бронирование
```bash
TOKEN="your_jwt_token_here"

curl -X POST http://localhost:8080/api/bookings \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "concert_id": "a34d04fb-290b-4485-8150-21a194666fb9",
    "seat_number": 42
  }'
```

### 5. Запуск Kafka Consumer
В отдельном терминале:
```bash
make consumer
```

Вы увидите логи обработки событий:
```
2026/01/29 15:30:42 Received booking event: booking_id=123...
2026/01/29 15:30:42 📧 Sending email notification for booking 123...
```

## Архитектурные решения

1.  **Асинхронные уведомления:** Вместо синхронной отправки email/push при бронировании, событие публикуется в Kafka. Это повышает отказоустойчивость и позволяет масштабировать сервис уведомлений независимо.
2.  **Repository Pattern:** Абстракция работы с БД позволяет легко заменить PostgreSQL на другую СУБД или добавить in-memory реализацию для тестов.
3.  **Валидация на уровне DTO:** Использование `go-playground/validator` для проверки входящих данных до вызова бизнес-логики.
4.  **Структурное логирование:** `log/slog` для JSON-логов, удобных для парсинга в ELK/Grafana Loki.

## Переменные окружения

Основные переменные в `.env`:

```env
# Server
PORT=8080

# PostgreSQL
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=booking_db
DB_SSLMODE=disable

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Kafka
KAFKA_BROKERS=kafka:9092
KAFKA_TOPIC=booking-events

# JWT
JWT_SECRET=your-secret-key-change-in-production
JWT_TOKEN_TTL=24h
```

## Масштабирование

Сервис готов к горизонтальному масштабированию:

*   **API сервер:** Можно запустить несколько инстансов за load balancer (nginx/traefik)
*   **Kafka consumers:** Consumer Group позволяет запускать несколько consumer'ов для параллельной обработки событий
*   **Redis:** Легко заменить на Redis Cluster для высокой доступности
*   **PostgreSQL:** Поддержка read replicas через настройку connection pool

## Будущие улучшения

*   [ ] Swagger/OpenAPI документация
*   [ ] Metrics (Prometheus) и Tracing (Jaeger)
*   [ ] Unit и Integration тесты
*   [ ] CI/CD pipeline (GitHub Actions)
*   [ ] Rate limiting middleware
*   [ ] Helm charts для Kubernetes
*   [ ] WebSocket для real-time уведомлений о доступности мест

## Лицензия

MIT
